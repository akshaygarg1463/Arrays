#include <bits/stdc++.h>
using namespace std;
int main() {

    vector<int>arr{1,2,4,1,3,3};
     vector<int>brr{6,4,5,3}; 
  int target =9;
    // vector<int>crr;
     for(int i =0;i<arr.size();i++){
     for(int j=0;j<brr.size();j++){
    if(arr[i]+brr[j]==target){
      cout<<"("<<arr[i]<<","<<brr[j]<<")";
    }
  
     }
    }
}
